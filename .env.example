# Supabase (leave blank to run in demo/mock mode with no backend)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Base URL of the FastAPI backend, once available.
# Not yet consumed by the frontend — reserved for lib/api when it is
# switched from the mock layer to real HTTP calls.
NEXT_PUBLIC_API_BASE_URL=
